#include <stdio.h>
#include <math.h>

int main() {

    float number = 2;

    //number = sqrt(number);
    //number = pow(number, 3);
    //number = round(number);
    //number = ceil(number);
    //number = floor(number);
    //number = abs(number);
    //number = log(number);
    //mber = sin(number);
    //number = cos(number);
    //number = tan(number);

    printf("%.2f\n", number);

    return 0;
}